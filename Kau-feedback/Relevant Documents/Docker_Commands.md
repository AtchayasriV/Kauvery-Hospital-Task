# Docker Commands

Build image:

```bash
docker build -t kau-feedback ./Kau-feedback
```

Run container:

```bash
docker run -d -p 8080:8080 kau-feedback
```

List images:

```bash
docker images
```

List containers:

```bash
docker ps
```
